/**
 * An enum for the direction of a swap.
 * @category AnchorDex Utils
 */
export enum SwapDirection {
  AtoB = "aToB",
  BtoA = "bToA",
}

/**
 * An enum for the token type in a AnchorDex.
 * @category AnchorDex Utils
 */
export enum TokenType {
  TokenA = 1,
  TokenB,
}
